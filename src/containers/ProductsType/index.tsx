import { Layout } from "shared/Layout";
import { ProductType } from "./ProductType";

export default function ProductTypeContainer(): JSX.Element {
	return (
		<Layout>
			<ProductType />
		</Layout>
	);
}
